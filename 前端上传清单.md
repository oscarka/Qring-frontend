# 前端文件上传清单

## ✅ 需要上传的文件

### 核心文件
- [x] `package.json` - 项目配置
- [x] `package-lock.json` - 依赖锁定文件
- [x] `tsconfig.json` - TypeScript 配置
- [x] `vite.config.ts` - Vite 配置
- [x] `index.html` - HTML 入口
- [x] `README.md` - 项目说明
- [x] `.gitignore` - Git 忽略文件

### 源代码
- [x] `App.tsx` - 主应用组件
- [x] `index.tsx` - 入口文件
- [x] `types.ts` - 类型定义
- [x] `utils.ts` - 工具函数
- [x] `constants.tsx` - 常量定义
- [x] `components/` - 所有组件文件
- [x] `services/` - API 服务文件

### 配置文件
- [x] `metadata.json` - 元数据（如果有）

## ❌ 不需要上传的文件

- `node_modules/` - Node 依赖（已添加到 .gitignore）
- `dist/` - 构建产物（已添加到 .gitignore）
- `.env` - 环境变量文件（已添加到 .gitignore）
- `.env.local` - 本地环境变量（已添加到 .gitignore）
- `*.log` - 日志文件（已添加到 .gitignore）

## 📝 Git 命令

```bash
# 1. 初始化 Git 仓库（如果还没有）
cd /Users/cc/qring-health-dashboard
git init

# 2. 添加远程仓库
git remote add origin https://github.com/oscarka/Qring-frontend.git

# 3. 添加所有文件（.gitignore 会自动排除不需要的文件）
git add .

# 4. 提交
git commit -m "Initial commit: Qring health dashboard frontend"

# 5. 推送到 GitHub
git branch -M main
git push -u origin main
```

## 🔍 检查清单

上传前请确认：

- [ ] `.gitignore` 文件存在且正确配置
- [ ] `node_modules/` 不会被上传
- [ ] `dist/` 不会被上传
- [ ] `.env` 文件不会被上传
- [ ] `README.md` 已更新
- [ ] `package.json` 中的信息正确

